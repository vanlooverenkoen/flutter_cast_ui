name: flutter_cast_ui
description: A plugin that simplifies the cast plugin by providing a basic U
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: be.vanlooveren.cast.ui.flutter_cast_ui
        pluginClass: FlutterCastUiPlugin
      ios:
        pluginClass: FlutterCastUiPlugin